{{- if and .Values.ports .Values.httpRoute.enabled .Values.gateway.public.enabled -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "generic-service.name" . }}-public
  labels:
    {{- include "generic-service.labels" . | nindent 4 }}
  annotations:
    dns.kian.sh/provider: cloudflare
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    {{- with .Values.gateway.public.tunnelTarget }}
    external-dns.alpha.kubernetes.io/target: {{ . }}
    {{- end }}
    {{- with .Values.httpRoute.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.public.name }}
      namespace: {{ .Values.gateway.namespace }}
  hostnames:
    - {{ .Values.httpRoute.hostname }}
  rules:
    - matches:
        - path:
            type: {{ .Values.httpRoute.pathType }}
            value: {{ .Values.httpRoute.path }}
      backendRefs:
        - name: {{ include "generic-service.name" . }}
          port: {{ (index .Values.ports 0).containerPort }}
{{- end }}
