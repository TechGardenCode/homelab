apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pihole-n2
spec:
  refreshInterval: 1h
  secretStoreRef:
    # This name must match the metadata.name in the `SecretStore`
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  data:
  - secretKey: EXTERNAL_DNS_PIHOLE_PASSWORD
    remoteRef:
      key: "8f0e0969-dfca-4e9c-a6e9-b33f01635716"
  - secretKey: EXTERNAL_DNS_PIHOLE_SERVER
    remoteRef:
      key: "a3b25b09-58cd-4b10-9113-b3480136d91c"