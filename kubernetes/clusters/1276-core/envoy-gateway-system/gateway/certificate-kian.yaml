apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: eg-tls-kian
  namespace: envoy-gateway-system
spec:
  secretName: eg-tls-kian
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "*.kian.sh"
