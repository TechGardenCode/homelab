apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: eg-tls-cert
  namespace: envoy-gateway-system
spec:
  secretName: eg-tls-cert
  issuerRef:
    group: certmanager.step.sm
    kind: StepClusterIssuer
    name: step-issuer
  dnsNames:
    - "*.core.internal.techgarden.gg"
