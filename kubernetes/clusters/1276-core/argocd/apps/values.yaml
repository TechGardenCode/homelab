applicationsets:
  main:
    name: main
    generators:
      - git:
          repoURL: https://github.com/techgardencode/homelab.git
          revision: homelab2
          directories:
            - path: "kubernetes/clusters/1276-core/**/*"
            - path: "kubernetes/clusters/1276-core/example/exclude"
              exclude: true
    template:
      metadata:
        name: "{{path[3]}}/{{path.basenameNormalized}}"
      spec:
        project: default
        source:
          repoURL: https://github.com/techgardencode/homelab.git
          targetRevision: homelab2
          path: "{{path}}"
          plugin:
            name: kustomize-helm
            parameters:
              - name: NAMESPACE
                string: "{{path[3]}}"
        destination:
          name: in-cluster
          namespace: "{{path[3]}}"
        syncPolicy:
          automated:
            selfHeal: true
            prune: true
          syncOptions:
            - ApplyOutOfSyncOnly=true