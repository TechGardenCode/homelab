apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: techgarden-bff
spec:
  refreshInterval: 1h
  secretStoreRef:
    # This name must match the metadata.name in the `SecretStore`
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  data:
  - secretKey: BFF_CLIENT_SECRET
    remoteRef:
      key: "833113c8-5db5-417f-9006-b347011ce6a8"