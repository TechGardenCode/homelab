apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - externalsecret.yaml
  - step-ca-root-configmap.yaml

configMapGenerator:
  - name: cloudflared-config
    files:
      - config.yaml
