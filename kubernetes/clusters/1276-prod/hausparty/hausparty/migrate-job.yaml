---
apiVersion: batch/v1
kind: Job
metadata:
  name: hausparty-migrate
  namespace: hausparty
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  completions: 1
  parallelism: 1
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      terminationGracePeriodSeconds: 30
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: migrate
          image: ghcr.io/techgardencode/hausparty:98a02ba0e40c3fa859a6f283584f8baabb94e8ab
          command: ["node", "scripts/migrate.mjs"]
          env:
            - name: PG_HOSTNAME
              value: "hausparty-db-rw.hausparty.svc.cluster.local"
            - name: PG_PORT
              value: "5432"
            - name: PG_DB
              value: "hausparty"
            - name: PG_USER
              value: "hausparty_app"
            - name: PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hausparty-db-app-credentials
                  key: password
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 128Mi
