apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: keycloak
  namespace: techgarden
spec:
  parentRefs:
    - name: eg-public
      namespace: envoy-gateway-system
  hostnames:
    - sso.techgarden.gg
  rules:
    - backendRefs:
        - name: keycloak
          port: 80
