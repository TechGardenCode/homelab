apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: eg-tls-coffee
  namespace: envoy-gateway-system
spec:
  secretName: eg-tls-coffee
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "kian.coffee"
    - "*.kian.coffee"
